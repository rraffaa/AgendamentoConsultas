import React from 'react';
import ReactDOM from 'react-dom';
import Root from './Root'; // Ajuste o caminho conforme necessário

ReactDOM.render(
  <React.StrictMode>
    <Root />
  </React.StrictMode>,
  document.getElementById('root')
);
